services:
  - type: web
    name: cerensafe
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --threads 4 --timeout 300 --keep-alive 300 server:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
